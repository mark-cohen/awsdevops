{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "mysqlDb": {
    "name": "mysqlDb",
    "connector": "mysql",
    "host": "${MYSQL_HOST_NAME}",
    "port": 3306,
    "database": "${MYSQL_DATABASE}",
    "user": "${MYSQL_USER}",
    "password": "${MYSQL_PASSWORD}",
    "charset": "utf8mb4",
    "collation": "utf8mb4_unicode_ci"
  }
}